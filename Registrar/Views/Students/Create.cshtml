@{
  Layout = "_Layout";
}

@model Registrar.Models.Student

<h1>Add a New Student</h1>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.StudentName)
  @Html.TextBoxFor(model => model.StudentName, new { required="required" })

  @Html.LabelFor(model => model.DateOfEnrollment)
  @Html.TextBoxFor(model => model.DateOfEnrollment, new { @type="date", required="required" })

  <input type="submit" value="Add a Student" />
}

<p>@Html.ActionLink("Show all Students", "Index")</p>