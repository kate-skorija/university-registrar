@* @using System.Collections.Generic;
@model List<Tattoo.Models.Artist>; *@

<div class="nav-bar-container">
  <nav class="navbar navbar-expand-sm bg-light navbar">
    <ul class="navbar-nav">
      <li class="nav-item">
      <div class ="weHateRazorStyling">
        <a class="nav-link" href='@Url.Action("Index", "Home")'>Home</a>
      </div>
      </li>
      <li class="nav-item">
        <a class="nav-link" href='@Url.Action("Index", "Artists")'>Available Artists</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href='@Url.Action("Index", "Clients")'>Booked Appointments</a>
      </li>
      @using (Html.BeginForm("Index", "Artists", FormMethod.Get))
      {
        <p>Search Artist: @Html.TextBox("firstName")</p>
        <button class="btn btn-outline-dark my-2 my-sm-0" type="submit">Search</button>
      }      
    </ul>
  </nav>
</div>

@* <form class="form-inline my-2 my-lg-0">
<input class="form-control mr-sm-2" type="search" placeholder="Stylists and Appts" aria-label="Search">
<button class="btn btn-outline-dark my-2 my-sm-0" type="submit">Search</button> *@


@* @using (Html.BeginForm())
{
    <p>First Name: @Html.TextBoxFor(model => model.FirstName)</p>
    <p>Last Name: @Html.TextBoxFor(model => model.LastName)</p>
    <p>Services: @Html.TextBoxFor(model => model.Services)</p>

} *@
